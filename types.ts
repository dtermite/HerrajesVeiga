
export interface Product {
  id: number;
  name: string;
  imageUrl: string;
}

export interface Brand {
  id: number;
  name: string;
  logoUrl: string;
}

export interface Slide {
  id: number;
  imageUrl: string;
  title: string;
  subtitle: string;
}
